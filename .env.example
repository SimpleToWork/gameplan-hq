ANTHROPIC_API_KEY=sk-ant-...
# Fireflies.ai transcript pull (api/fireflies.js) — key from Fireflies → Integrations → Fireflies API → Get API Key
FIREFLIES_API_KEY=
# Agents git sync (api/github.js) — a GitHub App installed on the org with Contents: read & write.
# GITHUB_APP_ID is the App's numeric ID; GITHUB_APP_PRIVATE_KEY is its PEM private key
# (paste with real newlines, or with literal \n — the function normalizes both).
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
