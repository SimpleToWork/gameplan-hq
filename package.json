{
  "name": "gameplan-hq",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy:rules": "node scripts/deploy-rules.mjs",
    "test": "node --test --test-name-pattern '.*' tests/*.test.mjs || node --test tests/branch-logic.test.mjs"
  }
}
